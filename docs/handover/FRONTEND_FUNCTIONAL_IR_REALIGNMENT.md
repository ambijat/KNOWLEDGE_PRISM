# Front-End Functional IR Realignment

Generated by Codex on 2026-07-07.

## Purpose

This pass realigns the KNOWLEDGE_PRISM front end with the Functional IR Interpretation Protocol sealed in block 18. The page is no longer only an evidence/disposition dashboard. It now foregrounds what sampled works do for International Relations explanation, with evidence grades shown as support annotations underneath.

No evidence grades, dispositions, ledger blocks, database promotion status, or ontology-core status were changed by this work.

## Files Changed

- `scripts/export_frontend_data.py`
  - Exports the persistent `functional_role` table.
  - Writes `public/data/functional_roles.json`.
  - Merges functional fields into `public/data/pilot_rows.json`.
  - Normalizes layer shorthand (`A`, `B`, `AB`, `AMB`) before matrix use.
  - Adds functional nodes and edges to `public/data/graph.json`.
- `index.html`
  - Loads `functional_roles`.
  - Merges functional roles into `pilotRows`.
  - Adds a new `Functional IR Interpretation` module.
  - Rewrites the selected-work panel so IR function appears before audit status.
  - Expands the graph lab to include IR function, interaction, concept family, ontology use, evidence grade, and disposition.
  - Exports functional fields in JSON/CSV and copied briefs.
- `public/data/functional_roles.json`
  - New generated static export.
- `public/data/pilot_rows.json`
  - Regenerated with functional fields on every pilot row.
- `public/data/project_status.json`
  - Regenerated with `functional_role_rows: 14` and `ledger_blocks: 19`.
- `public/data/ledger_blocks.json`
  - Regenerated through block 18.
- `public/data/graph.json`
  - Regenerated with functional graph structure.

## Generated JSON Files

- `public/data/pilot_rows.json`
- `public/data/functional_roles.json`
- `public/data/promoted_rows.json`
- `public/data/review_queue.json`
- `public/data/disposition_census.json`
- `public/data/ledger_blocks.json`
- `public/data/project_status.json`
- `public/data/evidence_dashboard.json`
- `public/data/pipeline.json`
- `public/data/graph.json`

## Functional Role Export Status

`functional_role` was exported successfully.

- Functional-role rows in DB: 14
- Functional-role rows in `public/data/functional_roles.json`: 14
- Pilot rows with populated functional roles: 14 of 18
- Pilot rows with functional schema fields present: 18 of 18

Each pilot row now exposes:

- `functional_ir_role`
- `interaction_illuminated`
- `substantive_layer_explanation`
- `ontology_use`
- `limitation_or_caution`

Rows without authored functional readings keep explicit empty functional fields and the caution: `No functional IR reading is recorded for this row yet.`

## How To Rebuild

From the project root:

```bash
python3 scripts/export_frontend_data.py
```

## How To Run The Local Server

```bash
python3 -m http.server 8765
```

Then open:

```text
http://127.0.0.1:8765/
```

## Verification Performed

- `python3 scripts/export_frontend_data.py`
- `python3 -m py_compile scripts/export_frontend_data.py`
- Embedded JavaScript syntax check with `node --check`
- Static JSON assertions:
  - 18 pilot rows
  - 14 functional roles
  - functional fields present on every pilot row
  - 19 ledger blocks exported
  - latest ledger block is block 18
  - graph includes `ir_function`, `interaction`, and `ontology_use` nodes
- Secret/path scan of `public/data`, `index.html`, and exporter.
- Playwright screenshot:

```bash
npx playwright screenshot --full-page http://127.0.0.1:8765/ /tmp/knowledge_prism_functional_ir.png
```

## How To Verify It Is No Longer Merely An Audit Dashboard

Open the page and check that the reading order is:

1. Hero statement: the project interprets sampled works by their function in IR explanation.
2. Selected Work panel begins with:
   - IR function
   - interaction illuminated
   - ontology use
   - substantive layer explanation
   - limitation/caution
3. The new `Functional IR Interpretation` section appears before the `Evidence Register`.
4. Evidence grade, thesis verdict, confidence, disposition, and quote validation appear after the functional reading, not before it.
5. The graph lab includes functional nodes, interaction nodes, concept-family nodes, and ontology-use nodes, not only grade/disposition nodes.

## Remaining Front-End Gaps

- The graph is still a static SVG layout and can become dense when all functional nodes are visible.
- There is not yet a dedicated filter by interaction type, contribution type, or ontology-use family.
- Functional-role export currently depends on the SQLite table; the block 18 note mentions `FUNCTIONAL_ROLE_TABLE.csv + .md`, but those files were not found on disk.
- Rows outside the 14 functional-role records still need authored functional interpretation if they are to appear as more than audit cases.
- No browser-side promotion or ledger writing exists, by design.

## Constraints Preserved

- No ontology promotion.
- No scaling to remaining bridge rows.
- No evidence-grade or disposition changes.
- No ledger block writes.
- No secret exposure.
